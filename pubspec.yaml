name: ecommerce_assignment_module_31
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish 

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.3.0
  firebase_crashlytics: ^5.0.6
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.5.4
  provider: ^6.1.5+1
  flutter_svg: ^2.2.3
  logger: ^2.6.2
  http: ^1.6.0
  gestures: ^1.0.0
  pin_code_fields: ^8.0.1
  carousel_slider: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/

